# ============================================================
# ملف التهيئة لمنصة Replit
# ============================================================

# تحديد اللغة والإصدار
language = "nodejs"
run = "npm start"

# إعدادات بيئة العمل
[env]
PORT = "5000"

# تحديد المجلدات والملفات الأساسية
[modules]
node = "18.18.0"

# إعدادات واجهة المستخدم (للتطبيقات المتعددة)
[web]
ports = ["5000"]
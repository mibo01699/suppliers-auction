// ============================================================
// الملف: backend/server.js
// الدور: الخادم الخلفي الرئيسي، متوافق مع Replit
// ============================================================

const express = require('express');
const cors = require('cors');
const bodyParser = require('body-parser');

const app = express();
const PORT = process.env.PORT || 5000; // استخدام PORT من البيئة أو الافتراضي

// Middleware
app.use(cors());
app.use(bodyParser.json());

// ============================================================
// واجهات برمجة التطبيقات (APIs) - (نفس الكود السابق)
// ============================================================

// ... (جميع واجهات API السابقة) ...

// ============================================================
// تشغيل الخادم
// ============================================================
app.listen(PORT, () => {
    console.log(`🚀 خادم GAP الخلفي يعمل على المنفذ ${PORT}`);
    console.log(`📦 APIs جاهزة للاستخدام: http://localhost:${PORT}/api`);
});
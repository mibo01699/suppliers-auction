{
  "name": "suppliers-auction",
  "version": "1.0.0",
  "description": "Suppliers Auction: Decentralized Bidding Protocol (Sandbox/Testnet)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "exit 0",
    "test": "jest --runInBand",
    "lint": "echo 'No linter configured' && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "jest": {
    "testMatch": ["<rootDir>/tests/**/*.test.js"],
    "testPathIgnorePatterns": ["/node_modules/"]
  }
}